<h1>Editing sector</h1>

<% form_for(@sector) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form' %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @sector %> |
<%= link_to 'Back', sectors_path %>
