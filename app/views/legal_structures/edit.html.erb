<h1>Editing legal_structure</h1>

<% form_for(@legal_structure) do |f| %>
  	<%= f.error_messages %>
	<%= render :partial => 'form' %>
  	<p>
    	<%= f.submit "Update" %>
  	</p>
<% end %>

<%= link_to 'Back to list', legal_structures_path %>
