<h1>New Member Organization</h1>

<% form_for(@member_org) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form' %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', member_orgs_path %>
