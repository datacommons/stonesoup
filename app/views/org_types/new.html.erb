<h1>New Organization Type</h1>

<% form_for(@org_type) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'form' %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', org_types_path %>
